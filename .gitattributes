# 저장소에 저장되는 줄바꿈은 항상 LF 로 통일한다.
# 윈도우에서 개발하고 리눅스(Dishost)에 배포하기 때문에, 통일해두지 않으면
# 파일을 한 번 열었다 저장하는 것만으로 전체 줄이 바뀐 것처럼 보이는 diff 가 난다.
* text=auto eol=lf

# 텍스트로 취급하면 안 되는 것들
*.png binary
*.jpg binary
*.ico binary
*.db binary
